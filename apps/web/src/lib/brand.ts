/**
 * Einzige Frontend-Markenquelle. Kein zweiter Pfad ueber
 * `public_config.app_name` (Backend-Wert dient nur der Drift-Erkennung,
 * siehe apps/api Contract-Test).
 */
export const BRAND_NAME = "AVENYTH";
